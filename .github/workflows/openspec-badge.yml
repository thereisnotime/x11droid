name: OpenSpec Badge

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: openspec-badge-${{ github.ref }}
  cancel-in-progress: true

jobs:
  badge:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: weAretechnative/openspec-badge-action@0ee70666841d4c08d3ec8a95b5546b91abc6125c # main
        with:
          # Only the spec-based metrics: the change-based ones (tasks_status,
          # open_changes) use OpenSpec commands deprecated in 1.2.0 and error
          # with 0 active changes. Re-add them once there are active changes.
          metric_types: number_of_specs,number_of_requirements
